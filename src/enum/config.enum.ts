export enum MySQLConfig {
  TYPE = 'type',
  HOST = '127.0.0.1',
  PORT = 'port',
  DATABASE = 'database',
  USERNAME = 'username',
  PASSWORD = 'password',
  SYNC = 'sync',
}

export enum AppConfig {
  HOST = 'host',
  PORT = 'port',
  LOG_ON = 'log_on',
}
